# Atajos personalizados para Bruno

# Navegacion rapida
alias ..="cd .."
alias ...="cd ../.."
alias h="cd ~"
alias proyectos="cd ~/eclipse-workspace"

# Comandos de sistema simplificados
alias actualizar="sudo apt update && sudo apt upgrade -y"
alias limpiar="sudo apt autoremove -y && sudo apt autoclean"
alias puertos="sudo netstat -tulanp"

# Git rapido
alias gs="git status"
alias ga="git add ."
alias gc="git commit -m"
alias gp="git push"
